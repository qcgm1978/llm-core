{
  "name": "llm-core",
  "version": "1.0.0",
  "description": "Core functionality for LLM service provider",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build",
    "publish:prepare": "echo \"Preparing to publish llm-core module...\" && cd ../ && mkdir -p .github/workflows && cp llm-core/.github/workflows/npm-publish.yml .github/workflows/"
  },
  "dependencies": {},
  "peerDependencies": {
    "@google/generative-ai": "^0.16.0"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}