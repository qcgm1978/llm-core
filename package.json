{
  "name": "@qcgm1978/llm-core",
  "version": "1.0.5",
  "description": "Core functionality for LLM service provider",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build",
    "publish:patch": "./publish.sh --patch",
    "publish:minor": "./publish.sh --minor",
    "publish:major": "./publish.sh --major",
    "publish:dry": "./publish.sh --dry-run",
    "publish:beta": "./publish.sh --tag beta"
  },
  "dependencies": {
    "@google/genai": "^1.7.0",
    "query-string": "^9.3.1",
    "sse.js": "^2.7.1",
    "uuid": "^13.0.0"
  },
  "peerDependencies": {
    "@google/generative-ai": "^0.16.0"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "typescript": "~5.8.2"
  }
}
